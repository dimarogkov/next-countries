const Loader = () => {
    return (
        <div className='fixed top-0 left-0 flex items-center justify-center w-full min-h-screen'>
            <span className='loading loading-spinner loading-lg text-dark dark:text-light' />
        </div>
    );
};

export default Loader;
